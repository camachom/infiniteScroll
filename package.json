{
  "name": "pinterestChallenge",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://camachom.github.io/infiniteScroll",
  "devDependencies": {
    "flow-bin": "^0.40.0",
    "react-scripts": "0.9.2"
  },
  "dependencies": {
    "gh-pages": "^0.12.0",
    "lodash": "^4.17.4",
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-modal": "^1.7.1",
    "react-pinterest": "^1.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "flow": "flow; test $? -eq 0 -o $? -eq 2",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
